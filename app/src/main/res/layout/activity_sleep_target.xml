<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
android:id="@+id/activity_sleep_target"
android:layout_width="match_parent"
android:layout_height="match_parent"
android:paddingBottom="@dimen/activity_vertical_margin"
android:paddingLeft="@dimen/activity_horizontal_margin"
android:paddingRight="@dimen/activity_horizontal_margin"
android:paddingTop="@dimen/activity_vertical_margin"
android:background="@drawable/background_gradient_new">

<TextView
    android:id="@+id/sleep_target_header"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="top"
    android:layout_margin="@dimen/text_margin"
    android:text="Choose your sleep target"
    android:textAlignment="center"
    android:textColor="@color/whiteTextColor"
    android:textSize="@dimen/text_header_font_size"
    android:textStyle="bold" />

    <TextView
        android:id="@+id/sleep_target_sub_header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="top"
        android:layout_marginTop="50dp"
        android:text="For most adults 6-9 hours is enough"
        android:textAlignment="center"
        android:textColor="@color/whiteTextColor"
        android:textSize="@dimen/text_other_font_size"/>

<ScrollView
    android:id="@+id/sleep_target_scroller"
    android:layout_width="match_parent"
    android:layout_height="250dp"
    android:scrollbars="none"
    android:requiresFadingEdge="vertical"
    android:layout_marginBottom="30dp"
    android:fillViewport="true"
    android:layout_gravity="center"
    android:gravity="center">
    <ListView
        android:id="@+id/sleep_target_list"
        android:layout_gravity="center"
        android:gravity="center"
        android:divider="@null"
        android:padding="5dp"
        android:scrollbars="none"
        android:layout_height="wrap_content"
        android:layout_width="match_parent"
        >
    </ListView>
</ScrollView>

<Button
    android:id="@+id/button_sleep_target_skip"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:layout_centerHorizontal="true"
    android:layout_gravity="bottom|center_horizontal"
    android:layout_marginBottom="85dp"
    android:background="@drawable/sleep_sense_button_transparent"
    android:padding="@dimen/button_padding"
    android:stateListAnimator="@null"
    android:text="Skip"
    android:textAllCaps="false"
    android:textColor="@drawable/button_text_color"
    android:textSize="@dimen/button_font_size"
    android:textStyle="bold" />

<Button
    android:id="@+id/button_sleep_target_continue"
    android:gravity="center"
    android:layout_below="@+id/button_sleep_target_skip"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_gravity="bottom"
    android:layout_margin="@dimen/button_margin"
    android:background="@drawable/sleep_sense_button_blue"
    android:padding="@dimen/button_padding"
    android:stateListAnimator="@null"
    android:text="Continue"
    android:textAllCaps="false"
    android:textColor="@drawable/button_text_color"
    android:textSize="@dimen/button_font_size"
    android:textStyle="bold" />
</FrameLayout>

